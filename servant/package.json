{
    "name": "servant",
    "version": "0.8.0",
    "description": "Servant for Whisker",
    "main": "src/servant.js",
    "dependencies": {
        "async-mutex": "^0.5.0",
        "commander": "^9.3.0",
        "csv-stringify": "^5.6.5",
        "generic-pool": "^3.9.0",
        "puppeteer": "^22.12.1",
        "whisker-web": "0.8.0",
        "yaml": "^1.10.2"
    },
    "scripts": {
        "build": "webpack --color --bail",
        "build:watch": "webpack --color --watch",
        "build:clean": "if [ -d dist ]; then rm -rf dist; fi && webpack --color --bail",
        "test": "jest --runInBand",
        "unittest": "jest test/unit/*.test.js --runInBand",
        "integrationtest": "jest test/integration/*.test.js --runInBand",
        "lint": "eslint .",
        "ci:lint": "eslint --quiet ."
    },
    "author": "",
    "license": "ISC",
    "type": "commonjs",
    "engines": {
        "node": ">=16.0.0"
    },
    "devDependencies": {
        "eslint-plugin-jest": "27"
    }
}
