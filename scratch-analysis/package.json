{
    "name": "scratch-analysis",
    "version": "0.1.0",
    "main": "index.js",
    "license": "GPL-3.0-only",
    "scripts": {
        "build": "rm -rf dist && webpack --progress --colors --bail",
        "test": "jest --forceExit --coverage --verbose",
        "test:coverage": "yarn test -- --coverage"
    },
    "devDependencies": {
        "eslint": "^7.32.0",
        "eslint-config-scratch": "^7.0.0"
    }
}
