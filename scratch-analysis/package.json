{
    "name": "scratch-analysis",
    "version": "0.6.0",
    "main": "index.js",
    "license": "GPL-3.0-only",
    "scripts": {
        "build": "if [ -d dist ]; then rm -rf dist; fi && webpack --colors --bail",
        "test": "jest --forceExit --coverage --verbose",
        "test:coverage": "yarn test -- --coverage"
    },
    "devDependencies": {
        "eslint": "^7.32.0",
        "eslint-config-scratch": "^7.0.0"
    }
}
